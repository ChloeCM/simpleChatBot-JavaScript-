function getDifference(marks) {
   // Calculate the difference between the first and last elements of the array
   let result = marks[0] - marks[marks.length - 1];

   // Output the result to the console
   console.log(result);
}
