// Write an arrow function named `square` that takes `num` as input
// and returns the square of `num`
const square = (num) => {
    return num * num;
}

//let result = square(5);
console.log(square(num));